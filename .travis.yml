dist: trusty
language: cpp
compiler:
    - gcc
    - clang
os:
    - linux
addons:
  apt:
    packages:
        - cmake
script:
    - mkdir build
    - cd build
    - cmake .. && make && ./test/truco-tests
